import { Button } from "@gravity-ui/uikit";

export const RemoveReportFromFavourite = () => {
  return (
    <Button size={"l"} view={"flat-danger"}>
      Удалить из избранного
    </Button>
  );
};
